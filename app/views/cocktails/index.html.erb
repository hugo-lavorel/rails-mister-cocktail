<div class="container">
  <h1>Welcome at The Bar</h1>
  <% @cocktails.each do |cocktail| %>
    <li><%= link_to "#{cocktail.name}", cocktail_path(cocktail) %></li>
  <% end %>
  <%= link_to "Add a Cocktail", new_cocktail_path, class:"btn btn-primary" %>
</div>
